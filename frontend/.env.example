# URL de base de l'API backend, suffixe /api inclus.
#
# En local, doit pointer vers le port du BACKEND (3000), pas celui du
# frontend (3001) — une erreur facile à faire, qui produit des 404 partout.
#   dev  : http://localhost:3000/api
#   prod : https://api.mymifa.com/api
NEXT_PUBLIC_API_URL=http://localhost:3000/api
